[python]
version = "3.12.0"

[build]
command = "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python -m DetectarPlacas"

[start]
command = "python -m DetectarPlacas"
